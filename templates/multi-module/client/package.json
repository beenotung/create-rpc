{
  "name": "client",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build:demo": "run-s build:upload build:chat",
    "build:upload": "esbuild --bundle test/upload-demo.ts > test/upload-demo.js",
    "build:chat": "esbuild --bundle test/chat-demo.ts > test/chat-demo.js",
    "test": "tsc --noEmit",
    "build": "tsc -p ."
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^24.11.0",
    "esbuild": "^0.27.3",
    "npm-run-all": "^4.1.5",
    "ts-node": "^10.9.2",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.9.3"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  }
}
